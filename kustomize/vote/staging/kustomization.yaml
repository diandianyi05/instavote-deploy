apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base
patchesStrategicMerge:
- deployment.yaml
- canary.yaml
# - service.yaml
replicas:
- name: vote
  count: 0
images:
- name: schoolofdevops/vote
  newName: hebeyi/vote
  newTag: main-ee999ffe-1712638294 # {"$imagepolicy": "instavote:vote:tag"}
commonLabels:
  project: instavote
  env: staging
commonAnnotations:
  supported-by: "iris@example.com"
# configMapGenerator:
#   - name: vote
#     literals:
#       - OPTION_A=STAGING
#       - OPTION_B=DEV
configMapGenerator:
- name: vote-options
  envs:
  - options.env
